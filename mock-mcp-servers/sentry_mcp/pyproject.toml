[project]
name = "mock-sentry-mcp"
version = "0.1.0"
dependencies = ["fastmcp"]

[project.scripts]
sentry_mcp = "sentry_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
