[project]
name = "mock-kubernetes-mcp"
version = "0.1.0"
dependencies = ["fastmcp"]

[project.scripts]
kubernetes_mcp = "kubernetes_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
