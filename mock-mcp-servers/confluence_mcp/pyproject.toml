[project]
name = "mock-confluence-mcp"
version = "0.1.0"
dependencies = ["fastmcp"]

[project.scripts]
confluence_mcp = "confluence_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
